# Datasets

Recorded radar/camera datasets and ROS bags live here during local development.

Do not commit large raw datasets directly into Git. Use Git LFS or an external release archive instead, and reference the dataset (name, date, conditions, size, download location) in [`docs/experiment_results.md`](../docs/experiment_results.md).
